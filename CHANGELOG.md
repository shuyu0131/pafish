# 更新日志

## [1.0.0] - 2026-08-02

首个正式版。核心系统 + 主题/插件生态 + 一键安装部署，可从零独立运行。

### 安装与部署

- 新增 `docker-compose.yml`：MySQL 8 服务编排，**数据卷持久化**（旧版 `docker run` 容器无卷，删除即丢数据），mysqladmin 健康检查
- 新增一键安装脚本：`scripts/install.sh`（Linux/macOS）与 `scripts/install.cmd`（Windows）——环境检查 → 启动 MySQL → 生成 `.env`（随机 `AUTH_SECRET`）→ `npm ci` → `migrate deploy` → 补建搜索索引 → 种子数据 → 生产构建
- 新增 `scripts/ensure-env.mjs`（幂等生成 `.env`）与 `scripts/fix-ft-index.ts`（幂等补建 FULLTEXT ngram 搜索索引）
- README 部署章节重写：一键安装（推荐）/ 手动部署 / 旧容器升级到持久化（mysqldump 迁移）/ PM2 常驻与 nginx 反代

### 文章与内容

- 文章全生命周期：草稿 / 发布 / **定时发布**（node-cron 每分钟检查，前台查询兜底）/ **置顶** / **回收站软删除**（恢复/彻底删除）
- Markdown 编辑器（@uiw/react-md-editor）：封面/摘要/分类/标签、**Ctrl+S 快速保存**、粘贴/拖拽传图、emlog 式"插入媒体"（本地上传或媒体库选择）
- **Markdown 批量导入**（≤50 个 .md，frontmatter 解析，重复 slug 自动 -2/-3）
- **相关文章推荐**（同分类 OR 共享标签，置顶优先）
- 页面（自定义模板）与导航/侧边栏组件系统（分类/最近文章/标签等）
- 分类树与标签管理；浏览量 7 天 cookie 去重上报

### 互动

- 评论系统：游客评论 + **嵌套回复**（最多 5 层）、审核队列（通过/垃圾/删除、回复上下文标注）、**评论分页**（20 条/页、`?cpage` 深链）、**点赞**（cookie 防刷）
- 三道反垃圾防线（UA 检测 / IP 5 秒限频 / 重复内容 1 小时判定）+ **游客图形验证码**（SVG 零依赖、亮暗主题自适应，后台可关）
- **登录身份评论**：已登录用户自动以登录昵称/邮箱发表（服务端强制、不可伪造），前台隐藏输入框
- **被回复邮件通知**（可选勾选）+ **站内通知中心**（未读徽标、单条/全部已读）
- 评论邮件通知（SMTP 发信失败静默不影响流程）

### 用户与认证

- 自研 JWT 认证（jose + httpOnly cookie，7 天）
- 角色体系：管理员 / 编辑；**开放注册**（后台可关）
- **注册邮箱验证码**（默认开启，后台可关）与**忘记密码邮箱验证码**（防枚举）；验证码 10 分钟有效、一次性、60 秒限频
- **后台 SMTP 可视化配置**（host/port/账号/密码/发件人，保存于服务器）+ **发送测试邮件**（未保存的表单值也能测）；`.env` 仅兜底
- 审计日志（登录/文章/评论/设置/角色变更全留痕，含 IP）

### 媒体

- 媒体库 `/admin/uploads`：统一管理多类型文件（图片/文档/压缩包/音视频）、按类型筛选与搜索、添加外部资源链接、复制 URL/删除
- **sharp 自动压缩**（PNG/WebP/JPEG，EXIF 校正，超 1920px 缩图），上传大小限制后台可配（默认 20MB）

### 主题与插件生态

- 主题系统（亮/暗模式、主题切换、Anatole 白色极简默认主题），内置 `paper`（纸感简约）与 `demo-nord` 主题
- **插件系统**：目录即应用零构建，事件钩子、前台注入、页面模板、插件前台页面
- **云存储后端扩展点** + **缤纷云存储插件**（AWS SigV4 手写签名零依赖，上传失败自动回退本地，删除媒体同步删云端）
- **应用商店**：内置官方源（GitHub Pages）+ 远程私有源（token 鉴权），在线安装/更新/卸载；商店包唯一顶层目录 + 路径穿越防护

### 后台体验

- 工作台仪表盘：近 14 天发文趋势与分类分布 SVG 图表、统计卡片
- 分组折叠侧边栏（内容/互动/外观/系统，展开状态记忆，移动端抽屉式）
- 文章列表多维筛选（分类/状态/置顶/回收站）与 5 种排序、批量操作（发布/转草稿/置顶/移动分类/回收站）
- 站点设置集中管理（站点信息/评论/邮件通知/注册/上传限制/商店地址）

### SEO 与前台

- `/sitemap.xml`（文章/分类/标签动态生成）、`/rss.xml`（最新 20 篇）、`/robots.txt`
- 文章动态 OG 标签（微信/微博分享卡片）
- 系统字体栈（无 Google Fonts 依赖，国内直连可用）；中文 slug 兼容

## [0.1.0] - 2026-08-01

内部开发版（未发布）。包含核心博客功能：文章/分类/标签/评论/后台管理/媒体上传等基础能力，此后演进为 1.0.0。
